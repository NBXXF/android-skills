interface:
  display_name: "Gradle Dependencies"
  short_description: "Gradle 依赖集中管理与版本目录强制化"
  default_prompt: "Use $aaaaa-xxf-gradle-dependencies to move hardcoded Gradle dependencies and version literals into libs.versions.toml, then flag any legacy violations with file and line references."
