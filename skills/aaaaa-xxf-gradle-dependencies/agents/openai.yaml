interface:
  display_name: "Gradle Dependencies"
  short_description: "Gradle 依赖、发布配置与密钥外提治理"
  default_prompt: "Use $aaaaa-xxf-gradle-dependencies to move hardcoded Gradle dependencies and plugin versions into libs.versions.toml, centralize reusable SDK/publishing/build configuration in gradle.properties, externalize credentials through Gradle properties or CI secrets, and flag legacy violations with file and line references."
