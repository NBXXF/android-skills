interface:
  display_name: "Android 打包助手"
  short_description: "交互或参数化打 Android APK/AAB 包"
  default_prompt: "Use $aaaaa-xxf-android-package-builder to ask one numbered question at a time: first build type, then channel if the project has flavors, then APK/AAB format. Never ask the user to provide all three items in one reply."
